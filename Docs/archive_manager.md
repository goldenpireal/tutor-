So the archive manager is gonna be having something similar to the file manager but named as archive_manager, but specifically built with just write , create and delete functionality , nothing more nothing less

If the user or the program want to access the informations inside the file manager it will be doing it through the file_manager(not archive_manager). 